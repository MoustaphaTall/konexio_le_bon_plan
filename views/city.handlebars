{{>header}}

<div class="container">
  <div class="row">
    <div class="col-12">
      <h1 class="product-list-title">Products in {{city}}</h1>
    </div>  
     

    
    {{#each products}}
    <div class="col-12">
      <div class="card mb-3 product-list"><a href="/products/{{_id}}">
        <div class="row no-gutters">
          <div class="col-md-4">
            <div class="card-img product-imgcontainer">
              <img src={{images.[0]}} alt="{{name}}" /> 
            </div>
          </div>

          <div class="col-md-8">
            <div class="card-body">        
              <h2 class="card-title">{{name}}</h2>
              
              <p class="card-text price"><strong>{{price}}€</strong></p>        
              <p class="card-text">{{description}}</p>              
            </div>
          </div>
        </div></a>
      </div>
    </div>        
    {{/each}}      
  </div>
</div>